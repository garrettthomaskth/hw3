figure(1)
epsilon=0.1;
alpha=1;
xSteps=100;
Tend=10;
ratio=0.1;
[Q,x,t,cons] = adv(xSteps,ratio,Tend,alpha,epsilon);
mesh(t,x,Q)
figure(2)
ratio=0.2;
[Q,x,t,cons] = adv(xSteps,ratio,Tend,alpha,epsilon);
mesh(t,x,Q)

%Q(:,1)
rotate3d on